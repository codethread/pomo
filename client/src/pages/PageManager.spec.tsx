import React from 'react';
import { render, screen } from '@test/rtl';
import { PageManager } from './PageManager';

describe('PageManager', () => {
  test.todo(
    'given the config loads, the page should transition from loading to the Pomodoro screen'
  );
});
